# Environment Configuration
NODE_ENV=development

# Database Configuration (Neon PostgreSQL)
# Get your connection string from: https://neon.tech/
# REQUIRED for inventory persistence and automatic migrations
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# xAI API Configuration
# Get your API key from: https://x.ai/
XAI_API_KEY=your-xai-api-key-here

# Primary model: grok-3-mini (fast, cheap, 131k context)
# Fallback model: grok-3 (powerful reasoning, slower)
# Previous models (deprecated): grok-2-latest, grok-beta
XAI_MODEL=grok-3-mini
XAI_FALLBACK_MODEL=grok-3
XAI_ENDPOINT=https://api.x.ai/v1

# CORS Configuration
# Frontend URL for CORS (use your Vercel URL in production)
CORS_ORIGIN=http://localhost:5173

# Clerk Authentication
# Get your keys from: https://clerk.com/
# REQUIRED for user authentication and data isolation
# Frontend publishable key (safe to expose in client-side code)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your-publishable-key-here
# Backend secret key (keep this secret, server-side only)
CLERK_SECRET_KEY=sk_test_your-secret-key-here
# Webhook secret for Clerk webhooks (optional, for user sync)
CLERK_WEBHOOK_SECRET=whsec_your-webhook-secret-here
# Clerk Account Portal sign-in URL (for OAuth redirects)
VITE_CLERK_SIGN_IN_URL=https://accounts.beartecai-inventory.uk/sign-in

# API URL (optional, defaults to /api in production)
VITE_API_URL=http://localhost:5173/api

# Feature Flags
# Speech Assistant (voice commands)
VITE_FEATURE_SPEECH=false

