# Environment Configuration
NODE_ENV=development

# Database Configuration (Neon PostgreSQL)
# Get your connection string from: https://neon.tech/
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# xAI API Configuration
# Get your API key from: https://x.ai/
XAI_API_KEY=your-xai-api-key-here

# Primary model: grok-3-mini (fast, cheap, 131k context)
# Fallback model: grok-3 (powerful reasoning, slower)
# Previous models (deprecated): grok-2-latest, grok-beta
XAI_MODEL=grok-3-mini
XAI_FALLBACK_MODEL=grok-3
XAI_ENDPOINT=https://api.x.ai/v1

# CORS Configuration
# Frontend URL for CORS (use your Vercel URL in production)
CORS_ORIGIN=http://localhost:5173

